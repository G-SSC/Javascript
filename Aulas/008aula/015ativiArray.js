let num = [5, 3, 4, 5, 6];

console.log(`O arrey de ${num.length} valores, contem os valores ${num}, para adicionar um novo valor, é apenas digitar "num.push(), e para colocar os valores do array em ordem crescente, é apenas escrever "num.sort"`)

for(let pos in num)
    {
    console.log(`A posição ${pos}`)
    }
    console.log(num.indexOf(2), num.indexOf(3))